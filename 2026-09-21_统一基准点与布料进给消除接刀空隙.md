# 2026-09-21 统一基准点、布料进给方向与接刀零间隙优化

## 变更背景与核心裁决
- **背景**：用户在母卷接续搭切时，发现两工位交界处（Y=5000~5260mm）出现一条约 260mm 的粉红空白生布横带，且指出右下角起切方向（自下而上）与布料拖拽进给方向（自上而下）存在几何脱节。
- **根因**：在 `PackingSolverService.java:200` 中，当基准为 `right-bottom` 时，执行了局部反转 `physY = rollL - y - h`。由于求解器从 $y=0$ 开始堆叠裁片，260mm 的未利用余量留在顶部，镜像反转后被强行甩到了工位顶端（即接刀入口处），导致工位 1（裁片截至 5000）与工位 2（裁片从 5260 起算）之间硬生生留出一条 260mm 的空白死区横带。
- **裁决**：统一母卷开卷排料流动方向，全面支持四大工业起刀基准原点（右上角、左上角、右下角、左下角），并提供【智能接刀进给 (0间隙)】一键步进与智能吸附，彻底消除工位交界处 260mm 悬空死区。

---

## 变更文件列表
1. `src/main/java/com/example/cutdemotwo/service/PackingSolverService.java`:
   - 解析四大基准：`origin.startsWith("right")`（靠右导轨量宽）、`origin.endsWith("bottom")`（自下而上起切）；
   - 在母卷连续开卷模式（`roll`）下，严格顺着送料流向紧贴工位入口排料（$physY = y + trim$），杜绝在工位接缝处倒置出 260mm 空白；
   - 仅在单板料头模式（`remnant`）且指定底部原点时，才将裁片倒贴至料头下底边；
   - 修正刀序 `cutSteps` 的横纵切坐标映射与有效扣料长度 `deductLen`。
2. `src/main/java/com/example/cutdemotwo/model/SolveRequest.java`:
   - 默认基准升级为 `right-top`（右上角 · 靠右导轨 · 顺流进给 · 服装纺织工业标准）。
3. `src/main/java/com/example/cutdemotwo/model/PieceDemand.java`:
   - 补充 `w`/`l`/`count` 别名与自动回退，提升多端反序列化鲁棒性。
4. `src/main/resources/static/index.html`:
   - 升级起刀基准下拉选择器，完整支持右上角、左上角、右下角、左下角四大工业基准；
   - 重构 CAD 机台工位 `bedStationGroup` 的准星、标牌与 $+X$（量宽）、$+Y$（进给/切削）坐标轴箭头渲染；
   - 状态栏光标坐标动态根据当前基准切换显示（距右导轨/距左布边，距进料口/距落料口）；
   - 新增【接续下一工位 (智能 0 间隙衔接)】一键步进按钮，自动计算上一工位末端有效裁切线并平滑推进工位；
   - 在母卷雷达条滑动吸附中，增加对上一工位有效切断线（如 4740mm）的高灵敏度磁吸（±800mm）。

---

## 验证结论
- 语法与构建：`mvn -DskipTests test-compile` 与 `mvn -DskipTests package` 成功，标记 `COMPILE_PROVEN`。
- 接口验证：HTTP POST `/api/solve` 使用 `right-top` 基准排料成功，有效裁片从 $y=0$ 开始紧密堆叠，两工位交界处 260mm 悬空死区彻底消除。
- 业务测试：UI 连续接续搭切与四向基准切换标记 `USER_TEST_PENDING`。
