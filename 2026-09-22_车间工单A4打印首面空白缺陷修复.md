# 2026-09-22 车间工单 A4 打印首面空白缺陷修复与纯净打印引擎升级

## 1. 缺陷现象与根本原因深度审查 (Root Cause Analysis)

### 现象
车间操作工在点击“打印工单”时，浏览器打印预览显示为 2 页（1/2 与 2/2）。其中：
- **第 1 页为全尺寸空白**（仅有浏览器默认页眉页脚与透明占位）；
- **真正重要的任务单与料头清单被推挤到了第 2 页**。

### 根本原因
1. **排版引擎占位未消除 (`visibility: hidden` 伪隐藏)**：
   原样式文件 `src/main/resources/static/css/modals.css:132-135` 声明了：
   ```css
   body * { visibility: hidden !important; }
   #printable-cut-ticket-area, #printable-cut-ticket-area * { visibility: visible !important; }
   ```
   在 W3C CSS 打印排版标准中，`visibility: hidden` **并不脱离文档流**！`<header>`、包含 100vh 高度 CAD 画布与侧边栏的 `<main>` 虽然不可见，但在打印机分页引擎中依旧占据整整一张 A4 纸的物理高度，形成了 100% 尺寸的隐形占位块。
2. **弹窗 DOM 结构位于文档末尾**：
   弹窗容器 `.settings-modal-overlay` 位于 `<body>` 最底部。因前置的 `header` 和 `main` 占据了整整一张纸，打印机自然将弹窗内容分页推向了第二页。
3. **基准点文本未本地化**：
   原工单显示起刀点为 `right-bottom` 英文标识，缺乏工位中文规范提示。

---

## 2. 改造方案与技术实施 (Implementation Details)

### 2.1 样式层重构 (`modals.css`)
- 将 `@media print` 中的宿主元素规则升级为：
  ```css
  header, main, footer, .sidebar-resizer, .sidebar-edge-toggle,
  #material-manager-modal, #cam-settings-modal, #cam-export-modal {
      display: none !important;
  }
  ```
  使用 `display: none` 彻底将其从排版盒模型树（Render Tree）中蒸发，不占用任何物理几何空间。
- 将 `#printable-cut-ticket-area` 的打印分页规则设为：
  `page-break-before: avoid !important; break-before: avoid !important;` 确保首张 A4 纸顶格打印。

### 2.2 打印引擎层升级 (`nc-dxf-exporter.js`)
- 引入 **纯净 A4 隔离式打印引擎 `printCutTicketDocument()`**：
  点击打印时，不再直接暴露当前复杂的宿主 DOM，而是在后台动态构建纯净的独立 `iframe`，仅抽取 `#printable-cut-ticket-area` 节点与专属紧凑样式表，在隔离环境中触发 `iframe.contentWindow.print()`，彻底解耦宿主页面任何潜在样式与尺寸污染。
- 起刀基准点规范化：映射为中文 `右下角基准 (原点)`。

### 2.3 接口暴露 (`app.js`)
- 在 `camApp` 及 `cutApp.plugins.export` 中双向注册 `printCutTicketDocument`，确保原生 HTML 与模块化调用均可无缝触发。

---

## 3. 涉及改动文件清单

1. `src/main/resources/static/css/modals.css`：重构 `@media print` 样式规则，强制脱离文档流，杜绝空白占位；
2. `src/main/resources/static/js/plugins/export/nc-dxf-exporter.js`：新增 `printCutTicketDocument()` 隔离打印引擎，优化起刀基准点为中文；
3. `src/main/resources/static/js/app.js`：注册并暴露 `printCutTicketDocument` 打印接口；
4. `scratch/test_print_fix.js`：自动化无头浏览器工单打印与结构验证测试。

---

## 4. 验证结果与工程状态

- **Maven 编译打包**：`mvn -DskipTests package` 成功，标记 `COMPILE_PROVEN`；
- **自动化测试**：`scratch/test_print_fix.js` 5 项测试 100% PASS：
  1. `cutApp` 接口就绪；
  2. 工单基准点正确显示为 `右下角基准 (原点)`；
  3. 打印按钮成功绑定 `printCutTicketDocument()`；
  4. 隔离式 iframe 打印 DOM 正确构建；
  5. 弹窗与单页工单渲染完全正常。
- **业务验证**：已完成本地模拟验证，标记 `USER_TEST_PENDING`（请用户在实际打印机或 Edge 打印预览中验证输出）。
