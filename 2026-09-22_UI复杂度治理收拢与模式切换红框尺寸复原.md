# 2026-09-22 UI复杂度治理收拢与模式切换红框尺寸复原记录

## 1. 变更背景与核心目标
- 针对用户反馈的“目前UI过于复杂，需要将非高频/物料管理下沉至二级页面展示，折叠收拢部分面板”以及“切换到料头又切换回母卷时，红框依然是料头的红框，未能恢复为母卷展开台面”两大关键痛点进行定向重构与缺陷修复。

## 2. 缺陷定位与根因分析
1. **模式切换机台红框尺寸残留缺陷**：
   - 污染位置：`src/main/resources/static/js/plugins/remnant/remnant-shelf.js:301-308`，装载料头时将 `data.bedL` 篡改为料头长度（如 1600mm）；
   - 恢复缺失：`src/main/resources/static/js/plugins/remnant/remnant-shelf.js:56-81`，`onMotherRollChange()` 仅重置幅宽与总长，遗漏了 `data.bedL` 的恢复，导致切回母卷后机台红框残留为料头的微缩尺寸；
   - 数据耦合：`state.js` 中缺乏工况数据隔离，料头工况直接覆写了母卷案例数据。

2. **界面信息过载与堆叠痛点**：
   - 左侧面板垂直铺满 5 个大卡片，机台标定（基准原点、修齐量、旋转、纵切）和瑕疵避让长表挤占了大部分首屏高度；
   - 母卷与料头的深度档案管理混在排产主界面；
   - 右侧面板纵向同时展开切刀表、料头入库表、面积对账表，滚动高度过大。

## 3. 实施变更清单
1. **工况隔离与红框尺寸复原**：
   - `src/main/resources/static/js/core/state.js`：在 `StateStore` 中新增独立的 `remnantWorkspaceData`，料头操作仅作用于独立工况区，实现与母卷案例的彻底解耦；
   - `src/main/resources/static/js/plugins/presets/scenarios.js`：在 `MOTHER_ROLL_SPECS` 中显式标定母卷标准台面 `bedL: 5000`；
   - `src/main/resources/static/js/plugins/remnant/remnant-shelf.js`：在 `switchCutMode("roll")` 与 `onMotherRollChange()` 中，切回母卷时强制恢复 `data.bedL = spec.bedL || 5000`，更新 `#inp-bed-l` 并重新触发自适应缩放与视口重绘，红框 100% 恢复为 5000mm。

2. **左侧面板卡片收折与二级下沉**：
   - `src/main/resources/static/index.html`：
     - `母卷型号与规格`：精简为单行批号与幅宽微标，标题栏提供 `[母卷档案 ↗]` 直达二级全景物料档案；
     - `机台起准点与展开标定`：默认加上 `collapsed` 折叠，标题栏显示动态摘要 `右下角 · 5000mm`；
     - `瑕疵避让记录`：默认加上 `collapsed` 折叠，标题栏显示动态微标 `共 8 处瑕疵` 与 `+ 增疵点`；
     - `料头货架库`：标题栏提供 `[料头档案 ↗]` 直达料头立体货架与代际追溯；
     - `预设工业工况`：改造为现代紧凑的 2x2 网格按键（`preset-grid`），点击秒切工况。
   - `src/main/resources/static/js/plugins/solver/quota-manager.js`：联动更新折叠卡片的摘要微标文本。

3. **右侧面板 Tab 分段选项卡化**：
   - `src/main/resources/static/index.html` 与 `layout.css`：
     - 引入顶部 Segmented Tabs：`[刀序与优化 (Cut Path)]` 与 `[料头与对账 (Balance)]`；
     - Tab 1 承载刀路优化卡、现场切刀表、G-Code/DXF 导出与工单打印；
     - Tab 2 承载料头入库表与母卷实切面积守恒对账单；
   - `src/main/resources/static/js/plugins/layout/splitter.js` 与 `app.js`：提供并暴露 `switchRightPanelTab()` 函数，实现选项卡流畅切换与徽标联动。

## 4. 自动化验证结果
- 执行 `scratch/test_ui_and_bug.js`（CDP 无头 Edge 浏览器）自动化断言：
  1. 初始化检查：`card-bed-origin` 和 `card-defects` 默认折叠收起，摘要标签正确；
  2. Tab 切换：右侧 Tab 1 与 Tab 2 自由切换，display 状态正确；
  3. 模式切换：从母卷切到料头，床台长度变为 1600mm；再从料头切回母卷，床台长度严格恢复为 5000mm，母卷需求与疵点零丢失，断言全部通过。
