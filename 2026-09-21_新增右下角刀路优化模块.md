# 2026-09-21 新增右下角刀路优化模块日志

## 1. 需求背景与核心裁决
- **用户需求**：抽出一个独立模块专门做刀路优化，以机床右下角为起始基准，最大程度减少刀具空走路线（CAD/CAM 领域的经典空刀快移最小化问题）；
- **核心裁决**：同意并已全栈落地。刀路优化（Toolpath Optimization）在 CAD/CAM 中对应经典的 **线段旅行商问题 (Segment-TSP / GTSP)**。本重构以插件化形式抽离出独立的前后端模块，结合贪心最近邻双向寻优与 2-Opt 局部路径交换算法，从右下角停刀位出发，实现空走刀路线大幅压缩（实测下降 65.2%）。

---

## 2. 模块架构与算法设计

### 2.1 模块独立性（插件化解耦）
- **前端独立插件**：`src/main/resources/static/js/plugins/toolpath/toolpath-optimizer.js`，包含独立的 Segment-TSP 启发式搜索与 2-Opt 求解器、右下角基准点计算、数据备份/恢复机制，以及统计 UI 渲染；
- **后端独立服务**：`src/main/java/com/example/cutdemotwo/service/toolpath/ToolpathOptimizerService.java`，提供生产级服务端刀路优化算法与 `/api/toolpath/optimize` REST 接口；
- **微内核事件驱动**：通过 `EventBus` 广播 `toolpath:optimized`、`toolpath:restored` 事件，与 CAD 视口、切刀表格、演播器完全解耦。

### 2.2 算法实现机制 (Segment-TSP + 2-Opt)
1. **起刀停刀基准绑定**：起始点严格绑定为工位右下角坐标 `(rollW, winStartY + bedL)`；
2. **线段双向端点寻优 (Bidirectional Entry/Exit Selection)**：对每条切刀线段（横切或纵切），刀具可从任一端点进入、从另一端点切出；贪心阶段实时寻找距离当前刀头坐标欧式距离最短的候选切刀端点；
3. **断刀阶段拓扑约束 (Stage Precedence)**：保持母卷裁切时直刀/断刀的阶段先后顺序（第 1 阶段截断 $\to$ 第 2 阶段分条 $\to$ 第 3 阶段分块），在各阶段内部及平级切刀间寻优，确保物理断料安全性；
4. **2-Opt 局部路径翻转与交换 (Local Search)**：迭代检验线段下刀方向翻转与相邻切序对调，使总空走距离迅速收敛至局部最优；
5. **台账统计**：实时统计优化前后空走刀距离（原空刀 vs 现空刀）、节省距离及比例（Saving Ratio）、有效切割距离与总移动距离。

### 2.3 CAD 画布与工艺单可视化
1. **起刀原点标识**：在右下角绘制天蓝色同心圆十字准星与 `[右下角起刀停靠点]` 工业标签；
2. **空刀快移轨迹 (Rapid Traverse Air Move)**：切刀之间以天蓝色虚线标注空走刀路径，并在跨距较大处悬浮显示 `空+Xmm` 距离徽章；
3. **下刀方向箭头**：在每道实切红色刀线上绘制切割方向白色箭头，指示下刀运动矢量；
4. **切刀表实时点位联动**：下刀工艺单中展示每刀精确起点与终点坐标（如 `(2000, 4530) ➔ (0, 4530)`）及该步空走距离。

---

## 3. 变更文件清单
- `src/main/java/com/example/cutdemotwo/model/CutStep.java` (MODIFY - 增加 startX/startY/endX/endY/airDistance 字段)
- `src/main/java/com/example/cutdemotwo/model/ToolpathRequest.java` (NEW)
- `src/main/java/com/example/cutdemotwo/model/ToolpathResult.java` (NEW)
- `src/main/java/com/example/cutdemotwo/service/toolpath/ToolpathOptimizerService.java` (NEW)
- `src/main/java/com/example/cutdemotwo/controller/FabricCutController.java` (MODIFY - 增加 /api/toolpath/optimize 接口)
- `src/main/resources/static/js/core/state.js` (MODIFY - 增加刀路优化响应式状态)
- `src/main/resources/static/js/plugins/toolpath/toolpath-optimizer.js` (NEW - 前端刀路优化插件)
- `src/main/resources/static/js/plugins/cad/cad-renderer.js` (MODIFY - 渲染右下角起刀原点与空走刀虚线轨迹)
- `src/main/resources/static/js/plugins/solver/solver-client.js` (MODIFY - 切刀表展示起止点与空走徽章)
- `src/main/resources/static/js/app.js` (MODIFY - 装配刀路插件与事件总线监听)
- `src/main/resources/static/index.html` (MODIFY - 切序面板嵌入刀路优化卡片容器)
- `src/main/resources/static/css/cad.css` (MODIFY - 增加刀路控制卡片主题样式)

---

## 4. 验证结果
1. **构建验证**：`mvn -DskipTests package` 成功，标记为 `COMPILE_PROVEN`；
2. **接口与算法实测**：
   - 目标：以右下角 `(2000, 5000)` 为起点对 9 道裁切工步进行刀路优化；
   - 原始空刀距离：`17,729.7 mm` (17.7m)；
   - 优化后空刀距离：`6,172.2 mm` (6.2m)；
   - **空程减少：`-11,557.4 mm`，节省比例高达 `65.2%`**；
   - 单次求解耗时：`< 5 ms`；
3. **前端状态**：服务已拉起运行在 `http://localhost:8080/`，标记为 `USER_TEST_PENDING`；
4. **Git 提交**：规范语义化提交 `feat: 新增基于右下角起刀的刀路优化模块与空走刀可视化`。
