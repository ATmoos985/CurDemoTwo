# 2026-09-21 代码解耦与微内核插件化重构日志

## 1. 重构背景与决策目标
原有工程存在以下实质性痛点：
1. **前端单文件膨胀与高耦合**：`src/main/resources/static/index.html` 达到 5180 行，样式、DOM、Konva 视口漫游、雷达 Scrubber、下刀动画、核销台账全部混合于同一文件内，任意修改一处均易引发非预期副作用；
2. **后端引擎硬编码**：`FabricCutController` 内部硬编码直接调用 `PackingSolverService` 和 `ScenarioOneService`，无法支持未来第三方优化器（如 OR-Tools、SCIP、启发式遗传算法等）即插即用；
3. **架构解耦要求**：实现微内核与插件化体系，确保各模块职责单一、各司其职，后续模块可独立升级、热插拔而不影响全局。

---

## 2. 详细解耦设计

### 2.1 后端：SPI 求解引擎策略工厂
- **统一接口**：`src/main/java/com/example/cutdemotwo/service/solver/ICutSolverEngine.java`，统一规范 `solve(...)` 与 `supports(...)` 契约；
- **策略注册工厂**：`src/main/java/com/example/cutdemotwo/service/solver/SolverFactory.java`，利用 Spring `List<ICutSolverEngine>` 自动装配所有求解器实例，支持根据入参特征（母卷模式、工位尺寸、算例参数）自动路由最佳求解器；
- **控制器瘦身**：`src/main/java/com/example/cutdemotwo/controller/FabricCutController.java` 仅依赖 `SolverFactory`，完全剥离具体业务求解算法实现。

### 2.2 前端：微内核 (EventBus + StateStore) 插件化体系
不引入 Webpack/Vite 等重型工程化脚手架，采用现代浏览器原生 ES6 Module (`type="module"`)：
- **微内核核心**：
  - `src/main/resources/static/js/core/event-bus.js`：轻量级发布/订阅总线，解耦所有组件间显式函数调用；
  - `src/main/resources/static/js/core/state.js`：全局响应式单例状态管理，收拢 `activeCase`、`activeMode`、`loadedRemnant` 等核心业务变量；
- **专职插件群**：
  - `presets/scenarios.js`：预设算例及母卷规格常量定义；
  - `settings/settings.js`：全局主题与工程参数持久化（偏好存储与加载）；
  - `layout/splitter.js`：视窗与侧边栏拖拽分割条、折叠面板交互；
  - `cad/cad-stage.js`：Konva Stage 视口管理、缩放平移漫游、光标追踪；
  - `cad/cad-rulers.js`：Canvas 毫米级标尺高精度自适应刻度绘制；
  - `cad/cad-renderer.js`：长卷底图、成品排料、余料料头、下刀切线、醒目红框工位实体渲染；
  - `radar/radar-scrubber.js`：全景雷达缩略条、磁吸抓取滑块、60FPS 高性能无级上下穿梭滚动；
  - `cut-player/cut-animator.js`：单步与连续下刀裁切演播器；
  - `solver/quota-manager.js`：裁片需求表格配额增删改、工位清除与排料状态重置；
  - `solver/solver-client.js`：异步调用 `/api/solve`，负责连续搭切零间隙衔接、面积台账汇总核销；
  - `remnant/remnant-shelf.js`：模式二余料料头货架、扫码装载与需求匹配；
  - `remnant/remnant-modal.js`：现场料头库弹窗与扫码器交互；
  - `app.js`：主装配器，初始化所有插件并向后兼容全局命名空间。
- **样式原子化拆解**：
  - `src/main/resources/static/css/base.css`：主题变量、重置样式、徽章与通用数据表；
  - `src/main/resources/static/css/layout.css`：Header、Main Layout、Sidebar、Splitter 与 Footer；
  - `src/main/resources/static/css/cad.css`：CAD 视口容器、标尺与浮动工具栏；
  - `src/main/resources/static/css/radar.css`：全景雷达条、刻度、疵点标记与平滑滑块；
  - `src/main/resources/static/css/shelf.css`：双工位模式切换、料头卡片与排料控制行；
  - `src/main/resources/static/css/modals.css`：设置中心与料头库弹窗。
- **精简 HTML 语义模板**：
  - `src/main/resources/static/index.html`：从 5180 行收敛至约 340 行纯语义化骨架，结构极其清爽。

---

## 3. 变更文件清单
- `src/main/java/com/example/cutdemotwo/service/solver/ICutSolverEngine.java` (NEW)
- `src/main/java/com/example/cutdemotwo/service/solver/SolverFactory.java` (NEW)
- `src/main/java/com/example/cutdemotwo/service/PackingSolverService.java` (MODIFY)
- `src/main/java/com/example/cutdemotwo/service/ScenarioOneService.java` (MODIFY)
- `src/main/java/com/example/cutdemotwo/controller/FabricCutController.java` (MODIFY)
- `src/main/resources/static/index.html` (MODIFY - 5180行 -> 340行)
- `src/main/resources/static/css/base.css` (NEW)
- `src/main/resources/static/css/layout.css` (NEW)
- `src/main/resources/static/css/cad.css` (NEW)
- `src/main/resources/static/css/radar.css` (NEW)
- `src/main/resources/static/css/shelf.css` (NEW)
- `src/main/resources/static/css/modals.css` (NEW)
- `src/main/resources/static/js/core/event-bus.js` (NEW)
- `src/main/resources/static/js/core/state.js` (NEW)
- `src/main/resources/static/js/plugins/presets/scenarios.js` (NEW)
- `src/main/resources/static/js/plugins/settings/settings.js` (NEW)
- `src/main/resources/static/js/plugins/layout/splitter.js` (NEW)
- `src/main/resources/static/js/plugins/cad/cad-stage.js` (NEW)
- `src/main/resources/static/js/plugins/cad/cad-rulers.js` (NEW)
- `src/main/resources/static/js/plugins/cad/cad-renderer.js` (NEW)
- `src/main/resources/static/js/plugins/radar/radar-scrubber.js` (NEW)
- `src/main/resources/static/js/plugins/cut-player/cut-animator.js` (NEW)
- `src/main/resources/static/js/plugins/solver/quota-manager.js` (NEW)
- `src/main/resources/static/js/plugins/solver/solver-client.js` (NEW)
- `src/main/resources/static/js/plugins/remnant/remnant-shelf.js` (NEW)
- `src/main/resources/static/js/plugins/remnant/remnant-modal.js` (NEW)
- `src/main/resources/static/js/app.js` (NEW)

---

## 4. 验证结论
1. **编译构建**：`mvn -DskipTests package` 成功（`COMPILE_PROVEN`）；
2. **静态资源伺服**：`GET /index.html` (200 OK)、`GET /css/base.css` (200 OK)、`GET /js/app.js` (200 OK)；
3. **接口端到端联调**：`POST /api/solve` 真实排料测试成功（返回完整 guillotine 阶段切刀、pieces 排列坐标、derivedRemnants 余料回收明细）；
4. **业务集成测试状态**：`USER_TEST_PENDING`（待在前端浏览器交互实测）。
