# 2026-09-21 接续搭切与需求自动核销优化记录

## 1. 变更背景与实质问题
用户在长布卷（60m）进行多次接续搭切排料时反馈：
1. **视口跳动**：下一次排料求解时画布产生突兀的跳变；
2. **前序成品变动/误删**：上一工位排好的裁片在下一次求解时部分丢失变动；
3. **需求重复求解**：求解器未按已安置裁片自动扣减，每次均以全量初始需求下发。

## 2. 查证定位与核心根因
1. **视口跳动**：`triggerSolve`（行 3893）与 `advanceBed`（行 3075）中无条件调用了 `resetToBedView()`，强制重置了 `stage.scale`（固定为 0.45 左右）与 `stage.position`，粗暴覆盖了用户的缩放和观察视口；
2. **裁片误删**：
   - 裁片保留过滤逻辑采用外边界判定 `p.y + p.l <= winStartY || p.y >= winEndY`；
   - 雷达拖拽缺少工位磁吸（Station Snapping），滑块容易停在 5.6m 等碎数位置；
   - 碎数停顿或浮点边界误差导致第一工位底部的裁片（`y+l=5000`）被判为工位内而直接抹杀；
3. **需求未自动扣减**：
   - Windows 环境下 Java 默认非 UTF-8 编码，导致 HTTP 报文中裁片品类中文名乱码；
   - 前端原先仅靠中文名称字符串模糊比对，乱码导致已排裁片与需求无法匹配，`completed` 无法增加，每次都发送初始满额需求。

## 3. 落地实施方案
1. **彻底消除视口跳动**：
   - 从 `triggerSolve` 与 `advanceBed` 移除 `resetToBedView()`；
   - 接续搭切完成后就地局部重绘，画布缩放比例与中心位置 100% 保持稳定；
2. **工位磁吸与裁片中心点保留**：
   - 在雷达拖拽释放（`onPointerUp`）增加工位磁吸：距工位倍数 ±800mm 优先吸附至工位整点（0, 5m, 10m...），否则按 500mm 网格栅格化对齐；
   - 裁片、切刀、料头保留逻辑重构为**中心点判定**（`p.y + p.l / 2` 落在 `[winStartY, winEndY)` 内才属于当前工位），彻底解决接缝公差误删；
3. **需求自动核销闭环**：
   - `application.properties` 配置 `server.servlet.encoding.force=true` 与 UTF-8；
   - `PlacedPiece` 与 `PackingSolverService` 增加 `demandId` 映射透传，CSV 读写使用 `StandardCharsets.UTF_8`；
   - 前端采用 `demandId` + 规格尺寸 $(w, l)$ + 品类名称三重匹配，确保 100% 正确核销；
   - 每次求解仅将 `remaining = totalCount - completed > 0` 的需求下发给求解器。

## 4. 涉及文件
- `src/main/resources/application.properties`
- `src/main/java/com/example/cutdemotwo/model/PlacedPiece.java`
- `src/main/java/com/example/cutdemotwo/service/PackingSolverService.java`
- `src/main/resources/static/index.html`

## 5. 验证结果
- Maven 单元测试与构建通过（`COMPILE_PROVEN`）；
- 模拟 3 个工位（0~5m, 5~10m, 10~15m）连续搭切，验证工位 1（8 件）、工位 2（8 件）、工位 3（7 件）共 23 件裁片 100% 完整保留，需求池待排件数从 180 严格递减至 165。
