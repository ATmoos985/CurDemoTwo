# 业务校准日志：全量统一机台默认基准原点为右下角

- **日期**：2026-09-21
- **类型**：Defect Fix / Consistency (`fix`)
- **状态**：`COMPILE_PROVEN` / `BROWSER_VERIFIED`

---

## 1. 缺陷现象与用户反馈
用户反馈：“我怎么发现我们的基准点还是这个红框的右上角，我不是说了右下角”。
经查，用户在前期明确提出了右下角起刀和刀路优化的业务要求，但系统自启动默认加载工况、全局默认值以及页面多处静态标签仍残留了右上角（`right-top`）配置，导致用户视觉上看到的红框原点瞄准位依然显示为“右上角”。

## 2. 根本原因定位与事实证据 (指名道姓)
- **事实矛盾 1（预设工况 4 硬编码）**：
  `src/main/resources/static/js/plugins/presets/scenarios.js:75`
  系统自启动默认加载的场景 4（60米母卷大段连续排料与全局疵点雷达）中，硬编码了 `cutOrigin: "right-top"`，覆盖了右下角设置。
- **事实矛盾 2（UI 与配置下拉选项写死选中项）**：
  `src/main/resources/static/index.html:128, 134, 624`
  左侧标定徽章静态写死为 `<span id="tag-cut-origin-header">右上角基准</span>`，两个 `<select>` 下拉框将 `right-top` 设为了 `selected`。
- **事实矛盾 3（状态栏与系统多处默认回退值滞后）**：
  `src/main/resources/static/index.html:538`
  底部状态栏静态写死为 `基准起点: 左布边 (0, 0) mm`，且在 `loadCase()` 触发时未同步调用 `updateUIInfo()` 刷新。
  `solver-client.js`、`cad-stage.js`、`cad-renderer.js`、`settings.js` 中所有原点取值的安全兜底值此前写死为 `|| "right-top"`。

## 3. 落地实施方案
1. **预设工况与数据模型全量统一**：
   在 `scenarios.js` 中将场景 4 的 `cutOrigin` 修改为 `"right-bottom"`；
2. **DOM 结构与默认 Selected 修正**：
   在 `index.html` 中将左侧面板徽标更新为 `右下角基准`（绿底高亮），下拉框 `<select id="sel-cut-origin">` 与 `<select id="cfg-cut-origin">` 默认选中项调整为 `right-bottom`，提示文案更新为推荐右下角起刀；
   底部状态栏默认文本与 `loadCase` 触发联动更新为 `靠右导轨·落料口起切 (2000, 5000) mm`；
3. **全局逻辑兜底收拢**：
   在 `app.js`、`settings.js`、`solver-client.js`、`quota-manager.js`、`cad-renderer.js`、`cad-stage.js`、`nc-dxf-exporter.js` 中，将所有回退兜底值彻底收敛为 `"right-bottom"`，并自动迁移本地 localStorage 中旧有的 `"right-top"` 缓存。

## 4. 验证结果
1. `mvn -DskipTests package` 构建成功（耗时 2.5s），标记 `COMPILE_PROVEN`；
2. Spring Boot 后台服务进程（Task `task-4468`，端口 8080）正常运行；
3. 通过真实 Edge 浏览器截屏验证：
   - 左侧面板徽章显示：`右下角基准`；
   - 下拉选项默认选中：`右下角 (靠右导轨 · 落料口自下而上起切 · 推荐)`；
   - 提示框显示：`标准起刀：右下角原点 (靠右导轨量幅宽，落料口自下而上起切，刀路最优距离起刀)`；
   - 右侧工艺顺序单顶部显示：`起刀停靠位: 右下角原点`；
   - 底部状态栏显示：`基准起点: 靠右导轨·落料口起切 (2000, 5000) mm`；
   - 刀序表与实切对账单 `100.0% 严密守恒` 同步刷新完成。
