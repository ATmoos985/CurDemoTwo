# 2026-09-21 变更记录：全量移除 Emoji 图标与工业 CAD 界面规范化

## 决策背景
针对用户提出的“把这些emj图标全部删除，看起来好丑”诉求，系统对 CAD/CAM 控制台进行全量去拟物化、去表情化改造，将原有分散在控制栏、模态框、状态提示与 Konva 渲染图层中的 49+ 处 Emoji 及方向性修饰符全部清除，替换为工业级 CAD 规范文本术语与方括号标签。

## 变更文件
1. `d:\GitHub\CutDemoTwo\src\main\resources\static\index.html`
   - 顶部导航与控制栏：
     - `📦 现场料头库` -> `现场料头库`
     - `🌓 浅色模式` / `🌙 深色模式` -> `浅色模式` / `深色模式`
     - `⚙ 设置` -> `设置`
     - `⚡ 执行 C++ 真实求解排料` -> `执行 C++ 真实求解排料`
   - 操作模式切换器：
     - 去除 `🌀` 与 `📦` 图标修饰，改为纯净工业单选切页。
   - 动作与视口快捷工具：
     - `⚡ 执行母卷开卷排料...` -> `执行母卷开卷排料...`
     - `⚡ 装载机台` -> `装载机台`
     - `🎯 机台视口` -> `机台视口`
     - `👁 进料展开` -> `进料展开`
     - `🌐 全卷全貌` -> `全卷全貌`
   - 工艺参数设置模态框（Settings Modal）：
     - `✕` -> `关闭`
     - `⚙ CAM 系统...` -> `CAM 系统与排料工艺配置中心`
     - `🖥 显示与常规` -> `显示与常规`
     - `✂ 工艺与切刀` -> `工艺与切刀`
     - `📦 料头管理策略` -> `料头管理策略`
     - `⚡ 求解器内核` -> `求解器内核`
     - `🔄 恢复默认值` -> `恢复默认值`
     - `💾 保存并应用` -> `保存并应用`
   - 浮动预警与操作反馈：
     - 去除 `✔`、`⚡`、`💡` 等图标前缀，规范为统一文本反馈。
   - Konva 画布渲染标注图层：
     - `✔ 已裁切完成区` -> `[已完成] 已裁切完成区`
     - `✂ 卷尾/底边修齐区` -> `[修齐切断] 卷尾/底边修齐区`
     - `⌖ 机台主基准点` -> `[基准原点] 机台主基准点`
     - `[0扣料保障 🛡]` -> `[0扣料保障]`
     - 标尺与矢量轴文本去除 `⟵`、`⟶`、`↑`，替换为标准工业标注 `[幅宽]`、`+X 轴向`、`+Y 轴向`。

## 验证与状态标记
- `.\mvnw.cmd test`：8 个业务单元测试 100% 通过，标记 `COMPILE_PROVEN`。
- `.\mvnw.cmd package -DskipTests`：打包成功，可执行 Spring Boot jar 正常拉起。
- 端口 8080 服务健康可用，经 Unicode 字符扫描确认为 0 残留 Emoji。
- 最终业务测试状态标记为 `USER_TEST_PENDING`。
