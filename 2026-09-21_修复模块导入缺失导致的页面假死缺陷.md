# 缺陷修复日志：修复模块导入缺失导致的页面假死缺陷

- **日期**：2026-09-21
- **类型**：Defect Fix (`fix`)
- **状态**：`COMPILE_PROVEN` / `BROWSER_VERIFIED`

---

## 1. 缺陷现象
用户进入系统页面后，点击任何交互按钮（包括“收起左栏/右栏”、“母卷与料头档案”、“导出机床代码”、“执行排料”、“设置”等）均毫无反应，系统如同卡死。

## 2. 根本原因定位与事实证据
通过 Headless Edge 浏览器的 CDP (Chrome DevTools Protocol) 控制台链路抓取定位：
- **位置 1**：`src/main/resources/static/js/app.js:37`
- **位置 2**：`src/main/resources/static/js/plugins/cut-player/cut-animator.js`
- **事实矛盾**：
  在早前将刀序演播重构为连续平滑真机 CAM 仿真的过程中，`cut-animator.js` 内部将播放和暂停命名为 `startContinuousSim` / `pauseContinuousSim`，移除了原有的 `playCuts` 和 `pauseCuts` 导出。
  但 `app.js` 第 37 行依然保留了 `import { stepCut, playCuts, pauseCuts } from './plugins/cut-player/cut-animator.js';`。
- **潜在后果**：
  在现代浏览器原生 ES6 模块加载规范下，模块在顶层静态解析阶段发现所依赖模块未导出该具名符号，直接抛出未捕获的全局语法错误：
  `SyntaxError: The requested module './plugins/cut-player/cut-animator.js' does not provide an export named 'pauseCuts'`
  这导致 `app.js` 主入口脚本完全停止执行，`window.camApp` 未能构建，向 `window` 全局对象批量挂载函数的逻辑从未触发。页面所有 `onclick="fn()"` 按钮点击时触发静默的 `ReferenceError: fn is not defined`，导致全站所有按钮处于彻底假死状态。

## 3. 修复方案
1. **补全向后兼容导出**：
   在 `src/main/resources/static/js/plugins/cut-player/cut-animator.js` 中重新导出 `playCuts` 与 `pauseCuts`，无缝桥接至 `startContinuousSim` 与 `pauseContinuousSim`。
2. **收拢主入口模块导入**：
   在 `src/main/resources/static/js/app.js` 中合并整理 `cut-animator.js` 的导入声明，消除分散与重复导入。
3. **编写并执行全工程模块静态分析脚本**：
   开发针对全部 21 个 ES6 模块的静态导入/导出交叉检验脚本，验证整个前端工程 `import` 与 `export` 契约 100% 闭环无缺失。

## 4. 验证结果
1. `mvn -DskipTests package` 构建成功。
2. 重启 Spring Boot 服务（PID 44480 停止，新进程启动）。
3. 真实 Edge 浏览器 Headless CDP 自动化探针评估：
   - 控制台零报错（0 Exceptions）；
   - 全量 21 个 JS 模块均返回 HTTP 200；
   - `window.camApp` 挂载成功（共 84 个核心方法）；
   - 模拟真实 DOM 触发 `toggleSidebar`、`openMaterialModal`、`openExportModal` 等按钮点击，状态正常展开与关闭。
