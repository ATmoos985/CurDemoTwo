# 变更记录：隐藏求解器细节并升级侧边栏边缘收折把手

- 日期：2026-09-22
- 任务：根据用户红框与黄框批注，彻底隐藏底层开源求解器细节，并将顶部收折按钮下放至左右边缘常驻把手。

## 变更文件清单

1. `src/main/resources/static/index.html`
   - 顶部 Header 工具栏：彻底删除 `[ 2D-Guillotine ]` 与 `[ C++ Solver ]` 标签；
   - 顶部 Header 工具栏：彻底移除 `[ 收左栏 ]` 与 `[ 收右栏 ]` 按钮；
   - 视口容器 `viewport-container`：左右边缘放置常驻胶囊收折把手 `edge-toggle-left` 与 `edge-toggle-right`；
   - 底部状态栏与参数弹窗：替换原 `PackingSolver (C++ 2D-Guillotine & HiGHS)` 为“智能几何排料内核”，约束标记为“直刀工艺校验通过”，直切级数改为“工业标准三阶段直切工艺”。

2. `src/main/resources/static/css/layout.css`
   - 重新设计 `.sidebar-edge-toggle` 样式：
     - 采用半圆胶囊贴边把手（16px × 56px），带 0.2s 平滑微交互与阴影悬停扩展；
     - 左侧把手贴紧左边框（圆角朝右），右侧把手贴紧右边框（圆角朝左）；
     - 支持深浅双色主题自适应。

3. `src/main/resources/static/js/plugins/layout/splitter.js`
   - 彻底解除边缘把手依赖顶部按钮的旧逻辑；
   - `toggleSidebar(side)` 逻辑重构：侧栏收折或展开时，把手常驻显示（`display: flex`），自动反转箭头指示（展开时左把手显示 `◀`，折叠后显示 `▶`；展开时右把手显示 `▶`，折叠后显示 `◀`）；
   - 保障折叠后一击展开，展开后一击收折，触发视口尺寸自适应事件。

4. `src/main/resources/static/js/plugins/presets/scenarios.js`
   - 预设算例中的求解引擎标识统一清洗为“智能几何排料内核”。

5. `src/main/resources/static/js/plugins/solver/solver-client.js`
   - 运行态状态栏更新与求解成功弹窗提示清洗：移除所有 C++、开源求解器名称，改为“智能几何排料计算成功”。

## 验证结论
- 编译状态：`COMPILE_PROVEN`（`mvn -DskipTests package` 构建成功）。
- 自动化验证：`COMPILE_PROVEN`（无头 Edge 浏览器 CDP 自动化测试 4 项用例 100% 通过）。
- 业务测试：`USER_TEST_PENDING`（等待现场业务实测）。
