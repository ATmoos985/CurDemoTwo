# 2026-09-22 工单排料图立式双栏同向重构与视角完全对齐

## 1. 业务决策与实质审查 (Decision & Root Cause)

### 现象与问题
用户指出：工单打印预览中的排样图与 CAD 屏幕上机台红框视口方向不一致。
- **屏幕真实视口**：机台物理红框为**立式竖放**（宽为水平 X: 0~2000mm，长为垂直 Y: 0~5000mm），左侧为通长边料 `REM-SIDE-01`，右侧为 2 列 3 排西装大片，右下角为起刀基准原点；
- **原工单图缺陷**：原先为利用横向宽度，强行做了 90 度旋转横卧，将长度映射到了水平轴、幅宽映射到了垂直轴，导致工人拿到工单时方位混乱（原左侧通长边料跑到了图纸上沿，顶排大片跑到了左侧），违背车间机台现场操作直觉。

### 裁决
**立即重构为立式 1:1 绝对同向蓝图，并引入“左立式蓝图 + 右信息清单”的紧凑双栏工业工单布局**。

---

## 2. 落地实施方案 (Technical Implementation)

### 2.1 坐标系 100% 绝对同构对齐 (`nc-dxf-exporter.js`)
- 重构 `generateCutTicketBlueprintSVG(data)`：
  - 幅宽 $X \in [0, 2000\text{mm}]$ 严格对应水平方向（左导轨在左，右导轨在右）；
  - 进料长 $Y \in [0, 5000\text{mm}]$ 严格对应垂直方向（顶部为进料端，底部为落料端）；
  - 左侧边料 `REM-SIDE-01` 严格在图纸左侧直立呈现；
  - 西装大片按 2 列 3 排整齐排列在右侧；
  - 避让 DEF-1/2 的横向带疵料头在排间自然横卧；
  - **右下角醒目标注 `★ 起刀基准原点 (0,0)` 与坐标轴指示箭头**，与屏幕红框右下角起切原点绝对吻合。

### 2.2 左右双栏工业级紧凑排版
- **左栏 (210px)**：机台工位立式排样图，按机台长宽比 1:2.5 等比缩放，内嵌 1.0m~4.0m 进料米数标尺与下刀线段；
- **右栏 (自适应)**：
  - 上部：工艺基本信息与刀路指标栏；
  - 中部：`一、合格成品裁片拣选清单 (Finished Pieces Checklist)`；
  - 下部：`二、回收料头入库贴标清单 (Remnant Inventory)`；
- **底端通栏**：现场三方签字栏（主裁操作工、品检质检员、仓库料头接收人）。

### 2.3 单页 A4 打印样式完美适配 (`modals.css`)
- 媒体查询中配置 `#ticket-grid-layout` 为网格两列并排；
- 限制立式 SVG 最大高度为 440px，整张工单总高度控制在 170mm 以内，留足 100mm 物理安全余量，绝不溢出到第 2 页。

---

## 3. 验证结果与工程状态

- **编译构建**：Maven 打包通过，标记 `COMPILE_PROVEN`；
- **自动化测试**：`scratch/test_print_fix.js` 执行 5 项断言全部通过（标记 `COMPILE_PROVEN`）；
- **实景截图对比**：生成 `cut_ticket_modal_with_blueprint.png`，与用户上传的屏幕图 `media_1790043469317.png` 实现了 100% 同向方位对齐；
- **业务验证**：标记 `USER_TEST_PENDING`。
