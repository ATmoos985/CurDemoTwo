# 缺陷修复日志：修复顶栏按钮溢出变形与浏览器强缓存导致的交互无响应

- **日期**：2026-09-21
- **类型**：Defect Fix (`fix`)
- **状态**：`COMPILE_PROVEN` / `BROWSER_VERIFIED`

---

## 1. 缺陷现象
用户刷新页面后，点击顶部及操作按钮依然没有反应，界面体验如同死锁。

## 2. 根本原因定位与事实证据 (通过真实 CDP 截屏与 Hit-Test 探针抓出)

### 事实矛盾 1：顶部 Header 按钮被 Flex 容器严重挤压变形并溢出视口
- **位置**：`src/main/resources/static/css/base.css:102` 与 `src/main/resources/static/css/layout.css:1-33`
- **测量数据证据**：
  通过无头 Edge 探针实际测量按钮尺寸与坐标：
  ```json
  {"text": "母卷与料头档案", "rect": {"left": 291, "top": -43, "width": 34, "height": 129}}
  {"text": "执行 C++ 真实求解排料", "rect": {"left": 899, "top": -58.5, "width": 44, "height": 160}}
  ```
- **事实矛盾与后果**：
  Header 中塞入了 13 个长文本按钮外加 3 个标签与 1 个长文档提示，总需求宽度超 1800px。
  而 `.tool-btn` 未配置 `white-space: nowrap;` 与 `flex-shrink: 0;`。在普通笔记本分辨率（如 1366x768 或 1440x900）下，按钮被强制压缩至仅 34px 宽，文本竖向排版拉伸至 160px 高，且垂直居中导致上边缘顶出视口（`top: -58.5px`）。
  用户鼠标点击时实际点到的是下方重叠区域或溢出的非活动区域，导致点击无任何响应。

### 事实矛盾 2：浏览器启发式强缓存旧的崩坏 JS 模块
- **位置**：`src/main/resources/application.properties` 与 `src/main/resources/static/index.html:739`
- **事实矛盾与后果**：
  Spring Boot 默认未禁用静态资源缓存，响应头无 `Cache-Control: no-cache`。用户在按 F5 刷新时，浏览器自动命中了本地缓存的旧 `cut-animator.js`，继续在后台抛出 `SyntaxError`，导致更新未生效。

### 事实矛盾 3：ES 模块异步延迟执行导致 `DOMContentLoaded` 事件丢失
- **位置**：`src/main/resources/static/js/app.js:310`
- **事实矛盾与后果**：
  若浏览器解析 ES Module 完毕时 `document.readyState` 已经为 `'complete'` 或 `'interactive'`，`window.addEventListener("DOMContentLoaded", ...)` 永远不会被调用，导致画布与控制器初始化逻辑被跳过。

---

## 3. 落地实施方案
1. **防止按钮变形与精炼顶部导航栏**：
   - 在 `src/main/resources/static/css/base.css` 为 `.tool-btn` 强制设定 `white-space: nowrap; flex-shrink: 0; height: 30px;`；
   - 在 `src/main/resources/static/css/layout.css` 为 `header` 设置自适应最小高度 46px 与 `overflow-x: auto`，`main` 布局采用 `flex: 1; min-height: 0;`；
   - 在 `src/main/resources/static/index.html` 浓缩按钮文本，按工业 CAM 逻辑划分为物料组、CAD组、导出组、视口组与执行组，将总宽度压缩至 850px。
2. **彻底击碎浏览器静态资源强缓存**：
   - 在 `application.properties` 添加 `spring.web.resources.cache.period=0` 与 `spring.web.resources.chain.cache=false`；
   - 在 `index.html` 的 `<head>` 添加防缓存 `<meta>` 标签；
   - 在 `index.html` 引入 `js/app.js` 时增加带时间戳版本号 `?v=20260921_2000`。
3. **加固应用程序自启动启动引导（Robust Bootstrap）**：
   - 在 `app.js` 中判断 `if (document.readyState === "loading")`，若已就绪直接执行自启动，绝不丢失初始化。

---

## 4. 验证结果
- **后端打包与重启**：`mvn -DskipTests package` 成功，Spring Boot 端口 8080 (Task `task-4293`) 正常运行；
- **真实浏览器 Hit-Test 探针检测**：
  全量 13 个顶部控制按钮与左侧切换按钮的 `hitSuccess` 均为 `true`，每个按钮坐标均位于 `top: 7.5px`，`height: 30px`，100% 处于可点击有效区域；
- **全站真实截屏确认**：通过真实 Edge 截屏（`scratch/header_layout_actual.png`），顶部导航栏字字清晰、排列整齐、各按钮功能点击均正常响应。
