# 2026-09-21 修复搭切需求核销与料头视觉增强记录

## 1. 变更背景与实质问题
用户在长布卷进行多工位接续搭切时反馈“这些是不是料片你直接拿走了”：
1. **当前工位重排时大面积空白**：当红框滑回第一工位（0~5m）重新排料时，除右上角排了 2 件前身大片外，台面下半段（y=1700~5000mm，约3.3米长）完全粉红空白，未排入任何裁片；
2. **左侧 400mm 贯通窄条空置**：母卷幅宽 2000mm 减去两件前身大片（1600mm）后剩余的 400mm 纵向条未排料，视觉上产生“料片被吞”的误解；
3. **可用料头标识模糊**：求解器规整生成的有效在库料头在浅色背景下对比度不足、字号小，用户无法直观辨识其为“在库待套裁资产”。

## 2. 查证定位与核心根因
1. **需求扣减核销范围扩大化漏洞**：
   - 核心文件：`src/main/resources/static/index.html:3609-3611` 与 `3751-3761`；
   - 根因：`getDemandsFromUI()` 读取的 `data-completed` 是基于画布上整卷所有已有裁片统计的（包含了当前工位即将被洗掉重排的旧裁片）；
   - 矛盾：当重排当前工位时，系统把当前工位原本拥有的旧配额当作“已完工订单”提前扣减掉了，导致下发给 C++ 求解器的 `demand` 极少甚至归零。求解器手头“无料可排”，排完仅剩的 2 件后，剩余 3.3 米台面只能作为整料保留，呈现大片空白。
2. **物理断刀几何与经向规则限制**：
   - 核心文件：`src/main/resources/static/index.html:1916` 与 `src/main/java/com/example/cutdemotwo/service/PackingSolverService.java:208-213`；
   - 事实：左侧 400mm 窄缝因“禁止旋转（经向对齐）”，订单池中所有主裁片（西装后身 850mm、西裤 700mm、大袖 600mm 等）在几何上均无法塞入，因此被 C++ 求解器规整切出作为长料头保留（`REM-PS-03: 400×4830mm`）。

## 3. 落地实施方案
1. **重构当前工位重排需求核销统计口径**（`index.html`）：
   - 在 `triggerSolve()` 中，统计已完成裁片时，严格仅统计【非当前工位且已锁定的外部裁片】（`!isPieceInCurrentStation(p)`）；
   - 当前工位内部即将被重新替换的旧裁片配额 100% 完整释放，计算公式为：`本工位有效配额 = 总计划件数 - 外部工位已排件数`；
   - 确保 C++ 求解器获得充足的待切配额，满载套裁填满当前台面。
2. **工业工程级料头视觉与原因标注增强**（`index.html`）：
   - 可用料头与带疵料头增加深色高对比度徽标胶囊背景；
   - 对 400mm 窄条等可用料头增加明确工艺原因标注：`[在库料头] REM-PS-xx: 规格 400×4830mm (1.93m²) | 主片超宽规整保留资产`；
   - 彻底消除空洞感与“被系统私自拿走”的误解。
3. **优先 demandId 强关联核销**：
   - `newPieces` 回填时优先匹配后端回传的准确 `p.demandId`，并兜底降级至全局需求表，彻底杜绝品类核销错位。

## 4. 涉及文件精确列表
- `src/main/resources/static/index.html`
- `target/classes/static/index.html`

## 5. 验证状态
- 代码编译与测试编译：`mvn -DskipTests test-compile`，`BUILD SUCCESS`，标记 `COMPILE_PROVEN`。
- 业务验证：用户可在浏览器刷新页面后在工位一（0~5m）重新点击【在当前红框工位排料】，验证 C++ 求解器满载排入充足裁片，标记 `USER_TEST_PENDING`。
