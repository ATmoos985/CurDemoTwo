# 2026-09-21 布料切割系统：料口管理与料头识别切割功能交付

## 一、业务决策与架构设计
依据《布料切割与料头管理方案_客户沟通版_排版优化.docx》第 4.1.2 节“排料算例2：料头优先利用，母卷扣料=0”与现场工业工位规范，系统完成从单一母卷裁切向双工位料口管理架构演进：

1. **工位料口分离设计**：
   - **PORT-01（母卷连续进料口）**：拉布机从 60m 母卷中拖曳面料至台面，裁切后扣减母卷长度（$\Delta L$），产出成品与一次料头。
   - **PORT-02（料头手工投料口）**：现场操作工将暂存区料头手工投入辅料口，锁定台面为料头物理尺寸，执行 C++ 真实排料，**母卷实切扣减强制锁定为 0mm**。原料头标记消耗，裁切剩余部分自动派生子料头。

2. **料头库存池与条码识别**：
   - 支持条码/二维码扫描识别（如 `REM-202609-001`）。
   - 包含规格、存放库位、面料批次、质量状态（完好 / 带疵需避让）、来源母卷等信息。

3. **订单需求智能逆向匹配推荐 (Smart Remnant Reverse Matching)**：
   - 接收到订单需求时，自动在库中逆向检索尺寸适合且面积最接近的在库料头，界面弹出高亮提示卡片，支持一键装载至 PORT-02 辅料口，实现母卷 0 消耗。

4. **二次余料派生与全生命周期溯源**：
   - 料头裁切后未用尽部分自动生成带有父料头血统的子料头（如 `REM-202609-001-SUB01`），支持在现场台账一键登记入库。

---

## 二、变更文件清单

| 文件路径 | 变更性质 | 核心修改说明 |
| :--- | :--- | :--- |
| `src/main/java/com/example/cutdemotwo/model/RemnantStock.java` | 新增 | 料头库存领域实体（ID、尺寸、库位、批次、状态、瑕疵列表、父子溯源） |
| `src/main/java/com/example/cutdemotwo/model/SolveRequest.java` | 修改 | 新增 `feedPortType`（"roll" / "remnant"）与 `sourceRemnantId` 字段 |
| `src/main/java/com/example/cutdemotwo/model/SolveResponse.java` | 修改 | 新增 `feedPortType`、`sourceRemnantId` 与 `derivedRemnants` 派生料头列表 |
| `src/main/java/com/example/cutdemotwo/service/RemnantService.java` | 新增 | 现场料头库存服务（内存并发池、条码扫码、智能逆向匹配、入库注册） |
| `src/main/java/com/example/cutdemotwo/service/PackingSolverService.java` | 修改 | 适配辅料口逻辑：料头投料时母卷扣料设为 0，自动派生子料头并绑定血缘 |
| `src/main/java/com/example/cutdemotwo/controller/FabricCutController.java` | 修改 | 暴露 `/api/remnants`、`/api/remnants/scan`、`/api/remnants/match`、`/api/remnants/register` |
| `src/test/java/com/example/cutdemotwo/FabricCutBusinessTests.java` | 修改 | 增加料头条码检索、智能匹配推荐、辅料口母卷 0 扣料单元测试（7/7 通过） |
| `src/main/resources/static/index.html` | 修改 | 前端料口切换 UI、现场料头库弹窗、扫码录入模拟、智能逆向推荐卡片、台账一键入库 |

---

## 三、验证记录
- **编译与单元测试**：`mvn clean test` 7/7 全部通过，标记 `COMPILE_PROVEN`。
- **服务运行**：Spring Boot 4.0.3 服务运行在 `http://localhost:8080/`。
- **API 实测**：
  - `GET /api/health` -> `packingsolverAvailable: true, remnantsInStock: 3`
  - `POST /api/remnants/scan` -> 精确识别 `REM-202609-001`
  - `POST /api/remnants/match` -> 成功匹配推荐最小在库料头
  - `POST /api/solve` (feedPortType="remnant") -> `deductLen: 0.0`，生成子料头 `REM-202609-001-SUB01/SUB02`。
