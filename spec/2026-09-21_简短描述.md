# 2026-09-21 变更记录：基准台右下角真位校正与设置页/黑白主题

## 变更文件列表
1. `d:\GitHub\CutDemoTwo\src\main\java\com\example\cutdemotwo\service\PackingSolverService.java`
   - 将 C++ 求解器坐标映射全面修正为真正右下角基准（$physX = rollW - x - w, physY = rollL - y - h - trim$）。
   - 裁切切断线、修齐切断线及带疵料头判定同步做对称映射。
2. `d:\GitHub\CutDemoTwo\src\main\resources\static\index.html`
   - CSS 注入 `[data-theme="light"]` 浅色 CAD 高对比度工程主题；
   - 顶部控制栏增加 `🌓 浅色模式 / 🌙 深色模式` 一键切换按钮与 `⚙ 设置` 按钮；
   - 增加 `#settings-modal` 全局工艺参数配置中心（显示常规、工艺切刀、料头策略、求解器内核 4 大分组卡片）；
   - 画布起刀原点修正至机台真正右下角（$originX = rollW, originY = winEndY - trim$），重置 $\leftarrow +X$ 与 $\uparrow +Y$ 矢量标注；
   - 预设场景 1~4 坐标全量纠偏贴合右下角基准；
   - 标尺引擎 `drawRulers()` 与画布引擎 `renderScene()` 实现主题自适应重绘。
3. `d:\GitHub\CutDemoTwo\src\test\java\com\example\cutdemotwo\FabricCutBusinessTests.java`
   - 编写并执行右下角坐标反演定位与母卷关联测试。

## 验证结论
- `.\mvnw.cmd test`：8 个测试全量通过，标记 `COMPILE_PROVEN`。
- 本地 8080 服务正常运行，标记 `USER_TEST_PENDING`。
