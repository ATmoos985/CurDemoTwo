# 2026-09-21 界面布局可拖动折叠与雷达1:1平滑防闪现拖拽优化审计

## 1. 变更背景与决策
用户在现场操作 CAM 控制台时反馈核心痛点：
1. **雷达滑块拖动闪现感**：拖动滑块时未能精确 1:1 跟随鼠标抓手位置，且鼠标松开时发生明显的白闪重排（“闪现感”）；
2. **面板边界不可调整**：左右侧面板宽度固定，无法拖拽边界分配 CAD 画布与参数面板的比例；
3. **卡片无法折叠、内容挤压在狭小固定框内**：左侧“裁片需求清单”被死锁在 `max-height: 220px` 产生局促滚动条，下方大片空白未被利用；右侧“料头入库登记”被死锁在 `max-height: 180px` 挤压。

**技术决策**：
1. **消除白闪的零重构架构**：
   - 彻底移除 `onPointerUp` 中的全量 `renderScene()` 树销毁；
   - 为 Konva 疵点节点打上 `defect-zone-*`、`defect-box-*`、`defect-txt-*` 具名属性；
   - 引入 `updateDefectVisualStates()` 与 `updateDefectRadarActiveState()`，在毫秒级就地更新图元属性与 DOM 状态，彻底消灭全量重排引起的白闪；
2. **1:1 绝对光标吸附与 RAF 节流**：
   - 抓取时精确计算光标相对滑块左边缘的像素偏移 `grabOffsetX = e.clientX - winRect.left`；
   - 全程开启 `setPointerCapture(e.pointerId)` 确保高速移动不脱手；
   - 结合 `requestAnimationFrame` 确保 60Hz/120Hz 刷新率下的极致跟手平滑；
   - 支持滚轮在雷达条上平滑推进/回退布卷；
3. **左右分栏双向可拖拽调节 (Resizable Splitters)**：
   - 引入 `.resizer-left` 与 `.resizer-right`（5px 宽，悬浮高亮蓝色，鼠标样式 `col-resize`）；
   - 支持动态调节宽度（左栏 260~650px，右栏 280~700px），双击重置默认宽度；
   - 宽度持久化至 `localStorage`；
   - 拖拽期间实时触发 Konva 画布与标尺的平滑 Resize；
4. **全卡片手风琴折叠与弹性空间撑满 (Flex Elastic Heights)**：
   - 为左右两边所有卡片实现折叠箭头角标（`▼` 展开 / `▶` 折叠，严格禁止 emoji 图标）；
   - 废除硬编码的 `max-height: 220px/180px`，切换为 `flex: 1 1 auto; overflow-y: auto;`；
   - 折叠非核心卡片时，需求清单和料头登记表自动弹性撑满整栏垂直高度；
5. **一键全景收拢 (Sidebar Toggles)**：
   - 顶部提供“收起左栏 / 收起右栏”切换按钮，并在画布视口两侧提供快捷浮动呼出小标。

---

## 2. 涉及变更文件清单
- `src/main/resources/static/index.html`：
  - CSS：增加 `.layout-resizer`、`.sidebar-edge-toggle`、`.panel-section.collapsible`、`.collapse-arrow` 及浅色主题样式；
  - HTML：为左右侧栏嵌入分割条、折叠箭头包裹层与弹性滚动容器；
  - JS：实现 `toggleSectionCollapse`、`toggleSidebar`、`initLayoutResizers`、`updateDefectVisualStates`、`updateDefectRadarActiveState` 及重构 `setupRadarInteraction`。

---

## 3. 验证与审计结论
- **编译与单元测试**：`.\mvnw.cmd test` 执行通过，8 个测试用例全部绿灯（`COMPILE_PROVEN`）。
- **打包与部署**：`.\mvnw.cmd package -DskipTests` 打包成功，Spring Boot 守护进程正常启动并在 `http://localhost:8080/` 成功提供服务。
- **交互验证**：等待用户现场拖拽与折叠体验（`USER_TEST_PENDING`）。
