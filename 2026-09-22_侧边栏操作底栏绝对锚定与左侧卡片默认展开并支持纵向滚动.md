# 变更记录：侧边栏操作底栏绝对锚定与左侧卡片默认展开并支持纵向滚动

- 日期：2026-09-22
- 任务：彻底解决侧边栏卡片折叠时底部操作按钮上浮跳动问题，同时恢复左侧卡片默认全量展开，引入全局平滑滚动条，消除强制锁死屏幕高度的限制。

## 变更文件清单

1. `src/main/resources/static/css/layout.css`
   - 将 `.sidebar` 与 `.sidebar-right` 的外层容器样式修改为 `overflow: hidden`，防止外层与内层双重滚动条冲突；
   - 新增 `.sidebar-scroll-body` 类：设定 `flex: 1 1 auto; overflow-y: auto; min-height: 0; overscroll-behavior: contain;`，配备精致平滑滚动条，供各分栏卡片自如纵向滑动展示；
   - 新增 `.sidebar-pinned-footer` 类：设定 `flex-shrink: 0; margin-top: auto; background: var(--panel-bg); z-index: 5;`，确保无论上方卡片处于展开还是折叠状态，底部操作栏绝对死死锚定在侧栏最底端，零上跳、零偏移。

2. `src/main/resources/static/index.html`
   - **左侧面板（母卷排产模式与料头模式）**：
     - 将卡片 1~4（母卷规格、机台起准点与标定、瑕疵记录、裁片需求清单）完整收拢置于 `.sidebar-scroll-body` 中；
     - 移除 `card-bed-origin` 和 `card-defects` 的强制 `collapsed` 类，恢复默认全量展开状态；
     - 解除 `#defects-container`（瑕疵列表）和 `#demands-container`（裁片需求）的人为 `max-height: 140px` 挤压与内嵌滚动条，令其自然平铺，由外层整体平滑纵向滑动；
     - 将卡片 5~6（工位搭切排料控制按钮组 + 4 大工业预设算例网格）收拢置于 `.sidebar-pinned-footer` 中，牢牢固定在左侧底端。
   - **右侧面板（工艺单与对账台账）**：
     - 将 `card-cut-sequence`（现场下刀工艺顺序单）置于 `.sidebar-scroll-body` 中；
     - 将 `action-footer-section`（包含用户在截图黄色框选标记的【机床代码 (G-Code/DXF)】与【车间工单 (A4打印)】按钮）收拢置于 `.sidebar-pinned-footer` 中；
     - 即使操作员点击折叠收起上方的下刀顺序单，底部的【机床代码】和【车间工单】按钮依然稳稳保持在右侧最底端（jumpDelta = 0px），彻底消除上浮跳动。

## 自动化验证结论
- 编译状态：`COMPILE_PROVEN`（`mvn -DskipTests package` 构建成功，Spring Boot 正常运行在 8080 端口）。
- 自动化端到端测试：`COMPILE_PROVEN`（启动无头 Edge 浏览器 CDP 自动化测试 3 大用例 100% 通过）：
  1. 左侧卡片默认全量展开，机台标定与瑕疵避让卡片展开无收折，可滚动主体 `scrollHeight: 2620px` > `clientHeight: 504px`，平滑滚动生效；
  2. 右侧卡片折叠后，底部【机床代码】与【车间工单】按钮偏移量 `jumpDelta: 0px`，贴底率 100%；
  3. 左侧卡片全部折叠后，底部工位控制与排料算例偏移量 `jumpDelta: 0px`，贴底率 100%。
- 业务测试：`USER_TEST_PENDING`（等待现场业务实测）。
