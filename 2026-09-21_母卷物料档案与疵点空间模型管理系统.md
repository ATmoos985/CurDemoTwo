# 母卷物料档案与疵点空间模型管理系统变更记录

**日期**：2026-09-21  
**性质**：业务架构升级与物料全生命周期管理系统落地 (`feat`)  
**涉及模块**：母卷业务建模、纺织4分制疵点精准空间表达、料头代际血统追溯与报废处置、物料管理中心前端插件

---

## 1. 业务决策与模型重构说明

### A. 母卷 (Mother Roll) 数据结构设计
在传统 CAD 只关注几何长宽的基础上，补齐纺织与柔性材料 ERP/MES 核心物料属性：
- **标识与工艺**：`rollId` (母卷条码/RFID)、`batchNo` (染色/织造批次号)、`supplier` (织造印染厂)、`materialName` (品名)、`color` (色号/色系)、`grammage` (克重 g/m²)、`composition` (面料成分比例)、`shrinkageRate` (预缩水率 %);
- **几何与动态结存**：`width` (有效净幅宽 mm)、`rawWidth` (毛边总幅宽 mm)、`totalLength` (标定总长 mm)、`currentRemainingLength` (动态结存剩余未切米数 mm)、`usedLength` (累计开卷实切米数 mm);
- **仓储质检**：`storageLocation` (立体库位)、`inspectionStatus` (验布状态 `PASSED`/`QUARANTINED`/`PENDING`)、`inspector` (验布员/视觉扫描机编号)、`inspectionDate` (检验日期)、`defects` (疵点全集清单)。

### B. 疵点 (Defect) 精准空间定位与工业 4 分制模型
- **空间几何坐标**：
  - `x`: 距靠山导轨的横向坐标 (mm)；
  - `y`: 距布卷起始端的经向纵深米数 (mm, $0 \le Y \le \text{TotalLength}$)；
  - `w`, `h`: 缺陷物理宽长；
  - `margin`: 工艺安全避让外扩裕量 (默认 20mm，阻绝刀尖撕裂纤维)；
  - `safeX`, `safeY`, `safeW`, `safeH`: 自动计算出的绝对安全隔离矩形；
- **工业标准分类与避让策略 (GB/T 与国际纺织 4 分制)**：
  - `HOLE`: 经纬向断裂破洞 (4分制扣4分, 绝对避让 `MUST_AVOID`, 规划切出料头隔离)；
  - `WEFT_DEFECT`: 断纬/抽纱/稀密档 (4分制扣3分, 强制避让 `MUST_AVOID`)；
  - `STAIN`: 印染滴油/黄斑水渍 (4分制扣2分, 外观避让，严禁进入正面合格裁片)；
  - `SLUB`: 粗节/死棉结头 (4分制扣1分, 允许贯通 `PENETRABLE`，可落入非关键余料)；
  - `SHADING`: 边中色差 (整幅对色，经向平行纹理控制)。

### C. 料头 (Remnant) 全生命周期与代际血统追溯
- `generation`: 记录料头代数（1=母卷直切一代料头，2=子料头套裁派生的二代料头）；
- `parentRollId` / `sourceRollId`: 直系母卷编号；
- `parentRemnantId`: 来源父级料头编号；
- `qualityGrade`: 质量评级 (`GRADE_A` 完好短料, `GRADE_B` 边角料, `GRADE_DEFECT` 局部带疵隔离料头)；
- `status`: `AVAILABLE` 在库, `IN_USE` 加工中, `CONSUMED` 已核销, `SCRAPPED` 报废。

---

## 2. 变更文件清单
- `src/main/java/com/example/cutdemotwo/model/Defect.java` (升级)
- `src/main/java/com/example/cutdemotwo/model/MotherRollInfo.java` (升级)
- `src/main/java/com/example/cutdemotwo/model/RemnantStock.java` (升级)
- `src/main/java/com/example/cutdemotwo/service/RemnantService.java` (增强)
- `src/main/java/com/example/cutdemotwo/controller/FabricCutController.java` (增强)
- `src/main/resources/static/js/plugins/material/material-manager.js` (新建)
- `src/main/resources/static/js/app.js` (修改)
- `src/main/resources/static/index.html` (修改)

---

## 3. 验证证据与状态标记
- **后端构建**：`mvn -DskipTests package` 构建成功，耗时 4.0s，标记 `COMPILE_PROVEN`；
- **接口验证**：
  - `GET /api/rolls` 正常返回母卷全属性与在库料头、疵点汇总；
  - `GET /api/rolls/ROLL-2026-0920` 正常返回母卷明细与 8 处标准 4 分制疵点；
  - `POST /api/rolls/ROLL-2026-0920/defects` 成功持久化新增疵点，状态码 200；
- **服务状态**：Spring Boot 后台任务 PID 44480 正常监听端口 8080，标记 `COMPILE_PROVEN`；
- **用户实操状态**：标记 `USER_TEST_PENDING`。
