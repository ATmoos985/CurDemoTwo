# 2026-09-21 浅色高亮CAD主题深度优化记录

## 1. 核心变更背景与决策
用户反馈“浅色你再优化一下”。分析发现先前浅色模式存在部分遗留的硬编码深色背景（如 `#1c1917`、`#27272a`、`#374151`、`#065f46`）、低对比度字体（如白字/灰字 `#d4d4d8` 在白底不可见、`#34d399` 在白底偏淡）、弹窗与货架背景未完全遵循 CSS 变量驱动以及 Konva 画布成品裁片底色过暗（原深绿块 `#065f46`）等实质问题。
本轮变更彻底实施 **工业 CAD 高对比度浅色蓝图规范 (Light Blueprint Theme)**，实现全链路 CSS 语义化变量与 Konva 自适应配色。

## 2. 变更文件与具体修改

### 文件：`src/main/resources/static/index.html`
1. **CSS 变量系统补全与微调 (`:root` 与 `[data-theme="light"]`)**：
   - 增加 `--card-blue-bg`, `--card-amber-bg`, `--remnant-active-bg`, `--badge-green-bg`, `--badge-green-color`, `--input-dark-bg`, `--input-dark-color` 等多套自适应变量。
   - 浅色模式下采用高对比度工业底色与字色：底色 `#f8fafc` / `#ffffff`，正文字色 `#0f172a` / `#334155`，沉稳墨绿成品边框 `#059669` 与青草绿柔和填充 `#ecfdf5`。
2. **硬编码组件样式抽取为语义类**：
   - `.section-card-blue`、`.section-card-amber`：驱动母卷卡片与料头货架自适应背景与边框。
   - `.card-inner-box`、`.callout-amber-tip`：替代原有内联 `#27272a` / `#1c1917` 容器。
   - `.btn-add-defect`、`.btn-add-demand`：解耦新增瑕疵与新增裁片按钮背景，亮色模式下对比度清晰。
   - `.dem-name`、`.remnant-dem`：裁片名称输入框去除硬编码颜色，亮色下高对比度呈现。
   - `.remnant-modal-dialog`、`.remnant-modal-header`、`.remnant-modal-body`、`.remnant-modal-footer`：解耦料头货架弹窗原有硬编码深色面板。
3. **Konva 画布工业级浅色 CAD 呈现 (`renderScene`)**：
   - 合格成品：浅色模式下填充 `#ecfdf5`（柔和薄荷白），边框 `#059669`（高精墨绿 CAD 线，2px），文字 `#065f46`，经纬向提示 `#059669`。
   - 可用料头与瑕疵料头：半透明琥珀色/天蓝色填充（`rgba(2, 132, 199, 0.14)`），边界鲜明。
   - 疵点避让区：半透明警示红框（`rgba(239, 68, 68, 0.15)`），暗红文字 `#991b1b`，绝不刺眼且醒目。
4. **料头匹配提示与决策弹窗自适应 (`checkAllDemandsRemnantMatch`)**：
   - 推荐料头提示栏采用 `.btn-action-use-rem` 与 `.btn-action-keep-roll` 样式类。
   - 提示徽章使用 `--badge-green-bg` 与 `--badge-green-color`，彻底消除黑底白字穿透。

## 3. 验证与状态
- **自动化测试**：`.\mvnw.cmd test`（8 个单元测试全部通过，耗时 5.18s）。
- **打包验证**：`.\mvnw.cmd package -DskipTests`（成功生成 `CutDemoTwo-0.0.1-SNAPSHOT.jar`）。
- **进程与服务**：后台常驻进程重启并保持监听 `http://localhost:8080/`，页面加载测试通过。
- **状态标记**：`COMPILE_PROVEN`，业务现场验证 `USER_TEST_PENDING`。
