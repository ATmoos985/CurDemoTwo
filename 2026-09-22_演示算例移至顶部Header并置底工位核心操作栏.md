# 演示算例移至顶部 Header 并置底工位核心操作栏变更记录

## 1. 变更背景与裁决
- **背景**：原左侧主面板底部常驻占用约 80px 的“预设工业工况快速载入（算例1~4）”网格属于示例演示数据，挤占了左侧母卷规格、起刀标定与瑕疵列表等关键生产配置区域的纵向空间。
- **裁决**：将示例工况迁出生产控制底座，在顶部工业导航栏（Header）“布料裁切 CAM”标题右侧新增下拉浮层选择器；左侧底座彻底移除算例卡片，原生产控制按钮（工位步进、智能接刀、在当前红框排料、清除/清空）直接落底锚定，释放近 90px 纵向操作视野。

## 2. 变更文件清单
- `src/main/resources/static/index.html`:
  - 在顶部 `<header>` 的 `.header-title` 内增加 `#preset-dropdown-container`，提供胶囊触发按钮 `#btn-preset-trigger` 与展开式 4 组工况菜单卡片；
  - 在母卷面板底部 `.sidebar-pinned-footer` 中移除预设算例紧凑网格，将工位控制按钮组绝对锚定在左侧栏最底端。
- `src/main/resources/static/css/layout.css`:
  - 调整 `header` 为 `position: relative; z-index: 100`，移除原有的 `overflow: hidden` 以免截断下拉浮层；
  - 新增 `.preset-dropdown-container`、`.preset-dropdown-trigger`、`.preset-dropdown-menu`、`.preset-menu-item` 等工业风格样式及深浅主题适配。
- `src/main/resources/static/js/plugins/solver/solver-client.js`:
  - 在 `loadCase(id)` 中使用 `classList.toggle('active', i === id)` 替代直接覆盖 `className`，兼容下拉菜单项高亮。
- `src/main/resources/static/js/app.js`:
  - 导出并挂载 `togglePresetDropdown`、`closePresetDropdown`、`selectPresetCase`、`updatePresetTriggerLabel` 方法；
  - 监听 `case:changed` 自动联动顶部触发按钮上的当前案例名称；
  - 注册页面外部点击与 ESC 快捷键自动收起菜单浮层。

## 3. 验证与测试
- 运行 CDP 无头 Edge 自动化回归测试 `test_preset_dropdown.js`：
  - Header 触发按钮与当前算例标签显示正确（默认算例4）；
  - 点击弹出浮层并展示 4 组算例，选择算例1后自动收起并切换视图数据，按钮文字变为“算例1: L形拆解”；
  - 侧栏最底部高度由 250px 缩减至 171px，与底边保持绝对贴合（偏移差 0px），主体卡片滚动区高度由 504px 扩展至 594px；
  - 点击页面外部区域能自如收起菜单浮层。
- 状态标记：`COMPILE_PROVEN` / `USER_TEST_PENDING`。
