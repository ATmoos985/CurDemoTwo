# 2026-09-21 修复瑕疵坐标镜像计算与图层压盖缺陷日志

## 1. 缺陷背景与根因
用户在界面中发现“长卷 3.2m 处明明有瑕疵，但排料裁片却没有避开且被覆盖”。经深度排查，定位到三处连锁缺陷：
1. **预设算例 Mock 数据错误**：`scenarios.js` 的场景二中，静态手工 Mock 的第 4 块成品直接占据 `X: [1000, 2000], Y: [3000, 3800]`，完全压死位于 `X: 1300, Y: 3200` 的第 2 处瑕疵，且切刀序列未做任何避让隔离；
2. **CAD 画布图层堆叠顺序倒置**：`cad-stage.js` 中 `defectGroup` 挂载在 `pieceGroup` 之下，导致裁片覆盖瑕疵时将底部的红色警示框、外扩虚线与文字完全遮蔽；
3. **后端坐标镜像计算缺陷**：`PackingSolverService.java` 中生成 `defects.csv` 时仅对 `"right-bottom"` 做了镜像，未识别推荐的 `"right-top"`，且纵向 Y 坐标错误使用了横向原点做翻转判定，导致真实求解在右上角基准下坐标镜像不一致。

---

## 2. 详细修复措施

### 2.1 后端：PackingSolverService.java
- 统一横向镜像逻辑：以 `origin.startsWith("right")` 决定 `safeX` 翻转；
- 统一纵向镜像逻辑：以 `isRemnantFeed && origin.endsWith("bottom")` 决定 `safeY` 翻转，长卷顺流开卷模式下 Y 轴保持与真实进料方向严格一致。

### 2.2 前端：cad-stage.js
- 调整 Konva 挂载层叠顺序为：`fabricBgGroup` -> `remnantGroup` -> `pieceGroup` -> `defectGroup` -> `cutGroup`；
- 确保瑕疵警示框和切刀轨迹始终处于最上层视线，即使发生异常重叠也绝不被任何实体裁片或料头遮盖。

### 2.3 前端：cad-renderer.js
- 为瑕疵实体框增加白色/警示黄色交叉斜线（Cross warning lines），无论在深色还是浅色模式下均提供极高对比度的视觉警示。

### 2.4 前端：scenarios.js & cloth_cad_nesting.html
- 将场景二手工错误 Mock 数据替换为真实 C++ PackingSolver 求解生成的合规排料数据：
  - 在 `Y: 1200~1580mm` 自动生成 `REM-DEF-01` 带疵料头（避让 #1 瑕疵）；
  - 在 `Y: 2780~3330mm` 自动生成 `REM-DEF-02` 带疵料头（避让 #2 瑕疵）；
  - 6 件 `西装前身-大片` 严密排列于完好无瑕区域，瑕疵避让率 100%，面积精确守恒。

---

## 3. 变更文件清单
- `src/main/java/com/example/cutdemotwo/service/PackingSolverService.java` (MODIFY)
- `src/main/resources/static/js/plugins/cad/cad-stage.js` (MODIFY)
- `src/main/resources/static/js/plugins/cad/cad-renderer.js` (MODIFY)
- `src/main/resources/static/js/plugins/presets/scenarios.js` (MODIFY)
- `src/main/resources/static/cloth_cad_nesting.html` (MODIFY)

---

## 4. 验证结果
1. **构建打包**：`mvn -DskipTests package` 成功，重新编译 16 个源文件，标记为 `COMPILE_PROVEN`；
2. **真实接口验证**：`POST /api/solve` 真实求解通过，在右上角基准下两处瑕疵 100% 被隔离入 `REM-PS-01` 与 `REM-PS-02`（`hasDefect: true`），与 6 件裁片零相交；
3. **前端业务验证**：已部署运行，标记为 `USER_TEST_PENDING`；
4. **Git 提交**：执行规范语义化提交 `fix: 修复瑕疵坐标镜像计算与图层压盖缺陷`。
