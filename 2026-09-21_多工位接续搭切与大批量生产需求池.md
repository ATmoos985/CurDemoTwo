# 变更审计日志：多工位接续搭切与大批量生产需求池核销

- **日期**：2026-09-21
- **状态**：`COMPILE_PROVEN` / `USER_TEST_PENDING`
- **提交信息**：`feat: 支持红框任意工位接续搭切增量保留与大批量订单需求核销`

---

## 1. 业务目标与变更背景
用户提出两大核心生产需求：
1. **接续搭切（Continuous Sequential Overlap Cutting）**：
   用户将大红框工位拖动至母卷任意展开区间（例如从 0~5m 移动至 5~10m，或 10~15m），触发排料时，之前已排好的合格成品、切刀步骤和回收料头必须完整保留在长卷全局坐标系上，新排料只针对当前工位区间进行增量更新或区间替换，绝不清除整卷。
2. **扩充多规格大批量生产需求池（Batch Production Demands）**：
   预置 10 大服装与工业部件真实裁片订单（涵盖西装前身、后身、西裤前片、后片、袖片、领衬、口袋布、腰头条等，总计划 188 件，总面积 ~100m²，完美匹配 60m 母卷）。
3. **订单核销跟踪与工位重置**：
   支持根据整卷累计成品动态核销订单（总需求/已切/待切），提供工位前后步进、工位清除与整卷清空重置功能。

---

## 2. 核心架构与实质修改

### 2.1 接续搭切增量合并算法 (`src/main/resources/static/index.html:3780-3825`)
- **原矛盾**：`data.pieces`、`data.cuts`、`data.remnants` 在求解成功后直接被 `result.*` 覆盖，导致历史工位裁切成果丢失。
- **改动后**：
  ```javascript
  // 1. 保留当前工位 [winStartY, winEndY] 外的历史裁片
  const retainedPieces = (data.pieces || []).filter(p => (p.y + p.l <= winStartY || p.y >= winEndY));
  const newPieces = (result.pieces || []).map((p, idx) => ({
      ...p,
      id: retainedPieces.length + idx + 1,
      y: p.y + winStartY
  }));
  data.pieces = [...retainedPieces, ...newPieces].sort((a, b) => a.y - b.y || a.x - b.x);

  // 2. 切刀轨迹合并与全局步序连续重编号
  const retainedCuts = (data.cuts || []).filter(c => c.type === "横切" ? (c.pos < winStartY || c.pos > winEndY) : (c.end <= winStartY || c.start >= winEndY));
  const newCuts = (result.cuts || []).map(c => ({
      ...c,
      pos: c.type === "横切" ? (c.pos + winStartY) : c.pos,
      start: c.type !== "横切" ? (c.start + winStartY) : c.start,
      end: c.type !== "横切" ? (c.end + winStartY) : c.end
  }));
  data.cuts = [...retainedCuts, ...newCuts];
  data.cuts.forEach((c, idx) => { c.step = idx + 1; });
  ```

### 2.2 真实服装多品类生产需求池与动态核销
- **需求扩充**：在 `SCENARIOS[4]` 中预置 10 类服装裁片（计划总数 188 件）。
- **核销闭环**：
  - `updateDemandCompletionFromPieces(data)`：从整卷累积 `data.pieces` 中严密统计各规格已切数量 `d.completed`。
  - `getDemandsFromUI()`：向求解器仅下发剩余待排件数 `demand = Math.max(0, count - completed)`。
  - UI 实时展示完成进度条与状态徽章：`已满额`（绿色）、`已切 X/Y`（蓝色）、`待排 0/Y`（灰色）。
  - 需求卡片标题实时显示总核销徽章：`[已切 X / 总 188 件]`。

### 2.3 多工位便捷导航与独立清除
- 增加 `[◀ 上一工位 (-5m)]` 与 `[下一工位 (+5m) ▶]` 快捷按钮，支持一键步进并自动平滑对齐视口中心。
- 增加 `[清除本工位排料]` 与 `[清空整卷已排]` 操作，便于反复演练多次切割与优化。

---

## 3. 验证与审计
- **自动化测试**：`mvn test` 执行 8 个测试用例全部通过（`BUILD SUCCESS`）。
- **HTTP 接口测试**：通过 Node.js 模拟调用 `/api/solve`，返回 `success: true`，生成 6 件成品、6 步直刀，面积 100% 严密守恒。
- **静态前端语法验证**：通过 Node.js 模拟浏览器上下文执行整体 JavaScript，无任何语法与初始化异常。
