# 2026-09-22 车间工单嵌入高保真排样搭切与起刀走刀矢量工程蓝图

## 1. 业务决策与实质审查 (Decision & Root Cause)

### 裁决
**坚决支持并立即加入**。在柔性材料数控裁切行业中，工单若仅有文字清单属于典型的“半盲工单”。车间主裁工面对长达数米的裁床机台，仅凭规格文字无法快速识别工件的物理几何位置，极易造成对称裁片拣选错乱、带疵料头误入良品库。必须在工单醒目位置嵌入直观、高保真的排料搭切工程蓝图。

### 事实与潜在隐患
- **位置**：`src/main/resources/static/js/plugins/export/nc-dxf-exporter.js:460-485`
- **原代码缺陷**：工单仅包含纯文字的基本指标栏、成品清单与料头清单，缺乏图形化指引。
- **潜在后果**：
  1. 裁片拣选贴标混乱（如左右对称件、大袖小袖混淆）；
  2. 料头核验定位困难（工人无法快速辨别机台上的 4 块料头物理方位）；
  3. 安全观察盲区（无法预判右下角起刀点的首刀下落与走刀路径）。

---

## 2. 落地实施方案 (Technical Implementation)

### 2.1 拒绝暗色位图费墨，采用纯白 A4 矢量工程蓝图
不直接截图当前暗色 CAD 视口（避免浪费墨水与缩放失真），而是在 `nc-dxf-exporter.js` 中开发专用的 `generateCutTicketBlueprintSVG(data)` 引擎：
1. **母卷与标尺**：按当前机台长度（5000mm）与幅宽（2000mm）等比缩放，带 1.0m~4.0m 进料标尺；
2. **成品裁片 (Pieces)**：高辨识度淡蓝底色 `#e0f2fe`，标注裁片名称、编号与尺寸（P1~P6）；
3. **回收料头 (Remnants)**：带疵料头标注醒目橙色虚线框 `[疵] REM-DEF-01/02`，完好料头标注绿色实线框 `[料] REM-SIDE-01/TAIL-01`；
4. **疵点安全隔离**：红色圆圈标出瑕疵位置 `! DEF-1/2`，清晰呈现避让下刀区；
5. **走刀工步线段 (Cuts)**：红虚线标注裁切刀线，带带圈数字 `①~⑨` 标注下刀加工工步；
6. **右下角起刀原点**：醒目红星与红色准星标注 `★ 起刀基准原点 (0,0)`，并标明 `X+ 导轨` 与 `Y+ 进料` 坐标轴箭头。

### 2.2 严格 A4 单页容积控制
- 排样图高度严格限制在 140px~160px，比例尺横卧全景 4:1 展开，左右占满纸张宽度；
- 配合紧凑表格，整份工单总高收敛在 160mm~200mm，留足 70mm 安全空白，绝不撑出第 2 页纸。

### 2.3 隔离式打印引擎同步支持
在 `printCutTicketDocument()` 内置打印样式表中，注入 `#ticket-blueprint-container svg` 响应式打印规则，保证激光打印机输出时 100% 矢量无损解析。

---

## 3. 验证结果与工程状态

- **编译构建**：Maven 打包成功，标记 `COMPILE_PROVEN`；
- **自动化测试**：`scratch/test_print_fix.js` 执行通过：
  - 矢量蓝图容器与 SVG 成功生成；
  - 6 件裁片与 4 块料头完整包含；
  - 右下角起刀原点标注成功捕获；
  - 独立 iframe 打印引擎完整提取矢量排样图；
- **业务测试**：生成屏幕真实截图 `cut_ticket_modal_with_blueprint.png`，标记 `USER_TEST_PENDING`。
